var numberOfChannels = 2;var azimuth_inc = 0.1;var zenith_inc = 0.0;if (jsarguments.length > 1) numberOfChannels = jsarguments[1];azimuth_inc = 2.0 / numberOfChannels;if (jsarguments.length > 2) azimuth_inc = jsarguments[2];if (jsarguments.length > 3) zenith_inc = jsarguments[3];// +1 for the bangoutlets = numberOfChannels + 1;function bang(){	outlet(0, numberOfChannels);}function anything(){	var args = arrayfromargs(messagename,arguments); 	var i;	for (i = 0; i < numberOfChannels; i++) {		outlet(i + 1, args[0], args[1] + i, args[2] + azimuth_inc* i, args[3] + zenith_inc * i, args[4], args[5], args[6])	}	// send a bang to the OpenSoundControl object	outlet(0, "bang");}